<thead>
    <tr data-query="each(head)">
        <th>{{name}}</th>
    </tr>
</thead>
<tbody data-query="each(products)">
    <tr>
        <td>{{name}}</td>
        <td>{{version}}</td>
        <td>{{link}}</td>
    </tr>
</tbody>